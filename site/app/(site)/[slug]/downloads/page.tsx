import { notFound } from 'next/navigation';
import { brands, brandBySlug } from '@/lib/brands';
import { pageMetadata } from '@/lib/metadata';

export function generateStaticParams() { return brands.map(({ slug }) => ({ slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const brand = brandBySlug((await params).slug); if (!brand) notFound(); return pageMetadata(`${brand.title} downloads`, `Download the ${brand.title} brand guide and asset collections.`, `/${brand.slug}/downloads/`); }
export default async function Downloads({ params }: { params: Promise<{ slug: string }> }) { const brand = brandBySlug((await params).slug); if (!brand) notFound(); const root = `/${brand.slug}/downloads/files`; return <div className="shell"><p className="eyebrow">Brand assets</p><h1>{brand.title} downloads</h1><p className="lede">Clearly organized source files for design, product, and implementation work.</p><ul className="download-list"><li><a href={`${root}/${brand.slug}-brand-guide.pdf`}><strong>Brand guide</strong><span>PDF standards and usage guidance</span></a></li><li><a href={`${root}/logos/svg/${brand.slug}-mark-color.svg`}><strong>Logo mark</strong><span>Primary SVG master</span></a></li><li><a href={`${root}/logos/svg/${brand.slug}-horizontal-color.svg`}><strong>Horizontal lockup</strong><span>Primary SVG master</span></a></li><li><a href={`${root}/favicons/favicon.ico`}><strong>Application icon</strong><span>Multi-size ICO file</span></a></li><li><a href={brand.specimen}><strong>Type specimen</strong><span>Outlined SVG reference</span></a></li><li><a href={`/${brand.slug}/brand/r/theme.json`}><strong>shadcn theme</strong><span>Installable registry JSON</span></a></li></ul></div>; }
