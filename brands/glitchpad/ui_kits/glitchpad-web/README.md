# Glitchpad web UI specimen

Open `index.html` directly in a browser. The static specimen demonstrates the dark product shell, dense file rows, a multi-representation inspector, path typography, and sulfur applied to selected rows, active views, and focus.

The viewport responds at tablet and phone widths. At narrower sizes the tree and metadata rail collapse so the file content remains primary.
