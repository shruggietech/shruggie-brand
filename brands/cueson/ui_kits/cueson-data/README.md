# Cueson data-interface specimen

Open `index.html` directly in a browser after generating the kit. The static specimen shows a source envelope, normalized cue rows, derived OCR status, exact timing, and preservation metadata on a responsive Archive Night surface.

This representative static specimen demonstrates the design system independently from the Cueson product implementation. It performs no network requests, executes no source media, and contains no user or tenant data.
