# Contract: Exploratory Candidate Matrix

## Inputs

Revision 2 compared exactly three named matrices: `sulfur-square`, `charcoal-square`, and `slate-square`. Revision 3 accepts the single bounded `context-switch` proposal combining Sulfur Square dark mode with Charcoal Square light mode. Every proposal declares complete dark, light, square, square-edge, page, fold, wordmark, reduced, monochrome, and platform dispositions. The rejected muddy deep-gold value is invalid input.

## Protected geometry

- Read canonical path data from `brands/glitchpad/brand.json` or current generated baseline SVGs.
- Assert the recorded geometry fingerprint before generating evidence.
- Enclose every protected page mark in the same safe square composition, including full, reduced, lockup, desktop, Android, and store samples.
- Apply only declared fill and square-edge changes between candidates.
- Keep path strings, square dimensions, safe inset, page scale, page placement, lockup transforms, and sample layout identical within each comparison context.

## Generated evidence

- Write the comparison document, raster capture, and measurement ledger only under ignored `dist/.s015-color-study/`.
- Label each direction, context, exact role values, measured relationships, and failing dispositions.
- Include current baseline beside the candidates where it materially helps the owner compare hierarchy.
- Mark every output as exploratory and not approved for production use.

## Failure behavior

- Fail closed on a geometry-fingerprint mismatch, missing baseline asset, malformed or prohibited color, incomplete role mapping, missing square enclosure, unsafe square inset, empty rendered bounds, or unrecorded comparison context.
- Do not modify production brand source or generated kits.
