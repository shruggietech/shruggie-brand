# Custom Asset Contract

The manifest is the only source of public custom asset inventory. All records are fully validated, including non-published records, because their committed source and ownership claims must be sound. Public output requires both an approved status and explicit publication eligibility. A `publication_eligible: true` record without approved status is rejected, not silently published. Publication eligibility does not hide files already committed to a public repository; unapproved private review artwork should stay outside public source control.

`source.path` is a brand-relative file under the committed brand tree. Absolute paths, URI schemes, `..`, backslash escapes, symlink escapes, missing files, query strings, fragments, wrong magic/extension, stale SHA-256, active or animated SVG, external CSS URLs, truncated raster files, and unsupported public formats fail validation. Every SVG `url(...)` reference must point to a local fragment. Raster custom sources require Pillow for complete decoding. A published source used by `authoritative_inputs` may be referenced without altering that record. A non-published asset cannot also be a required canonical source or live in a tree always copied to hosted downloads (`logos`, `favicons`, `icons`, `specimens`, `guidelines`, or `nextjs/registry`). The public kit retains eligible sources at their declared paths, and the hosted download mirror copies those bytes unchanged. Public archives omit non-published source files and declarations, then rebind the manifest and consumer provenance to the filtered `brand.json`.

Every published record appears once in the PDF, portable gallery, hosted Expressions and atmosphere topic, and the asset library/download inventory. PDF expression pages hold at most two cards and repeat as needed rather than clipping a growing inventory. No topic or category exists for an empty eligible set. Public previews are contained, never cropped or recolored; their declared well gives adequate text/control contrast. Attribution and license accompany each downloadable entry.
