# Specifications

Feature and release slices use sequential `NNN-slug` directories produced by the repository-installed Spec Kit workflow. Each completed slice keeps its specification, plan, tasks, and verification evidence together.

| Slice | Status | Purpose |
| --- | --- | --- |
| 001 | In progress | Establish the brand-system repository, migrate the five kits, build CI and releases, publish the registry site, configure DNS, and retire the staging folder safely. |
