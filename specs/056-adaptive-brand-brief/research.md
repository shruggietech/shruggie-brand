# Research and Decisions

## Existing approvals

Decision: Keep the two existing source-bound ledger entries. `approval_ledger.gate_1` requires production source and derivative configuration; `gate_2` binds approved derivatives and public surfaces. These are already enforced by `brand_contract.py`, `identity_continuity.py`, and `verify.py`. A new brief or palette gate would contradict the owner decision.

## Provisional review

Decision: Describe Gate 2 as private provisional review, with social image and copy identified separately from wide and stacked lockups. The private pattern in `scripts/render_eso_weave_gate2.py` demonstrates review while public projection remains disabled. Final public compilation before Gate 2 would invert approval order.

## Social copy migration

Decision: Capture exact slogan, optional description, layout, and approved line breaks during discovery. Do not reinterpret `descriptor` or `brand_idea`; #281 records mixed meanings across eight brands and owns their migration.

## Entry-point synchronization

Decision: Update `skill/SKILL.md`, then regenerate `skill/AGENTS.md` through `sync_agents_md.py`. The hosted manual reads the reference inventory, so revising `03-interview.md` updates published guidance without a separate site copy.
