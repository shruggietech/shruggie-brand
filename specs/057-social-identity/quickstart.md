# S057 Validation Quickstart

1. Review source fields and compare go-schedule's new primary element list to its prior reduced list.
2. Build all eight kits with `python scripts/build_all.py`; require zero verifier problems and zero glyph failures.
3. Review generated Gate 1 go-schedule lockups and Gate 2 social image contact sheet with the owner, then record exact approved proof and derivative binding.
4. Run Python contract/pipeline tests, site lint/build/test, release contract and publication audits from `.github/workflows/build.yml`.
5. Inspect provenance, downloads grouping, kit/site PNG hashes, OG/Twitter metadata, legacy links, crop readability, and repository Social Preview upload inventory.
