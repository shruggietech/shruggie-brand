# Contract: Documentation Migration Disposition

## Source inventory

Every Markdown file directly under `skill/references/` must appear exactly once in `manual_pages` and `source_dispositions`. Nested provenance, brand README, Spec Kit, and code documentation remain with their existing owners and are recorded as source-family dispositions where they are part of the current documentation corpus.

Each source disposition records the current source or source family, owning surface or provenance boundary, preserve/consolidate/generate/redirect/retire action, destination when content is retained, and concise rationale.

Unlisted top-level reference Markdown fails site preparation and documentation verification.

## Route inventory

Every generated route with kind `docs-index`, `docs-page`, `guidelines`, `guidelines-topic`, or `downloads` must resolve to exactly one route disposition. S041 preserves all five route kinds at their current paths.

Adding or removing a relevant route kind without updating the documentation contract fails. A future redirect or retirement must include an explicit destination and test evidence.

## Navigation

Manual page positions must be unique. Pagination order must be contiguous. Required topic coverage must be complete. Hosted topic hierarchy remains generated by the existing brand portal contract.

## Scope preservation

The migration must not implement #193 light-first presentation, #194 expressive asset modeling, or #202 integration-preview repair. References to those issues remain coordination notes, not hidden implementation.
