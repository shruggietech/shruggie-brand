# Contract: Generated Guideline Catalog

1. Load current logo provenance and icon manifest after their generators complete.
2. Reject an inventory record whose declared file is absent.
3. Group deliveries by semantic design, never by size alone.
4. Select the largest available suitable preview and retain all delivery rows.
5. Embed representative content for offline viewing and emit normalized relative links marked `data-kit-asset`.
6. Emit stable section and group IDs, semantic headings, tables, and explicit skipped-capability notices.
7. Derive all color formats from canonical HEX using fixed precision and provide copy buttons whose labels include brand, token, and format.
8. Scope complete token maps to both dark and light comparison wells.
