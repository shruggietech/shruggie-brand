# Contract: Guideline Publication Context

1. Portable generated HTML contains no `All brands` host exit.
2. Every portable kit asset link uses a normalized relative path and the `data-kit-asset` marker.
3. Site preparation rejects traversal, absolute, URL, or missing asset targets.
4. Site preparation rewrites marked links to the exact copied `/<slug>/downloads/files/` destination.
5. Site preparation injects exactly one neutral `All brands` footer link to `/` and does not add a site shell, mark, promotion, or redirect.
6. Metadata and affiliation copy remain governed by their existing contracts.
